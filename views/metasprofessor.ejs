<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
    />

    <title>META TRACKER</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background: linear-gradient(to right, #ff7e5f, #feb47b);
        color: #484848;
        display: flex;
        justify-content: space-between;
        align-items: center;
        height: 100vh;
        margin: 0;
        padding: 0;
        position: relative;
        overflow: hidden;
      }
      .left-side {
        background-image: url("img/az.jpg");
        background-size: cover;
        background-position: center;
        box-shadow: 0px 0 20px rgba(0, 0, 0, 0.458);
        width: 30%;
        height: 90%;
        display: flex;
        border-end-end-radius: 30px;
        border-start-end-radius: 30px;
        justify-content: center;
        align-items: center;
        animation: slideRight 2s forwards;
      }
      @keyframes slideRight {
        from {
          transform: translateX(-100%);
        }
        to {
          transform: translateX(0%);
        }
      }
      .right-side {
        width: 70%;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        padding: 20px;
      }
      .logout-button {
        position: absolute;
        top: 10px;
        right: 10px;
        border-radius: 25px;
        animation: fadeIn 2s forwards;
      }
      @keyframes fadeIn {
        from {
          opacity: 0;
        }
        to {
          opacity: 1;
        }
      }
      .button-group {
        display: flex;
        flex-direction: column;
        align-items: center;
        width: 70%;
        animation: fadeIn 2s forwards;
        padding: 20px;

        border-radius: 15px;
      }
      .btn-primary {
        transition: all 0.3s ease;
        box-shadow: 0px 0 15px rgba(0, 0, 0, 0.196);
        width: 475px;
        height: 70px;
        font-size: 1.5rem;
        margin: 30px 0;
        border: none;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 15px;
        position: relative;
        overflow: hidden;
      }
      .btn-primary::after {
        content: "";
        position: absolute;
        top: 0;
        right: 0;
        width: 3%;
        border-radius: 17px;
        height: 100%;
        background-color: #5874c8;
        transition: width 0.3s ease;
      }
      .btn-primary:hover::after {
        width: 30%;
      }
      .btn-em-aberto {
        color: #000000;
        background-color: rgba(255, 255, 0, 0.51);
      }
      .btn-concluido {
        color: #000000;
        background-color: rgba(0, 128, 0, 0.586);
      }
      .btn-em-atraso {
        color: #000000;
        background-color: rgba(255, 0, 0, 0.603);
      }
      .btn i {
        margin-right: 10px;
      }
      .img-fluid {
        max-width: 80%;
        height: auto;
      }
      .title {
        font-size: 2.5rem;
        margin-bottom: 10px;
        text-align: center;
        animation: fadeIn 3s forwards;
      }

      .button-container {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        margin: auto;
        animation: fadeIn 2s forwards;
        padding: 50px;
        background-color: rgba(255, 255, 255, 0.625);
        border-radius: 40px;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.225);
      }
      @keyframes fadeIn {
        from {
          opacity: 0;
        }
        to {
          opacity: 1;
        }
      }
    </style>
  </head>
  <body>
    <div class="left-side">
      <img src="img/icon_txt.png" alt="Icon" class="img-fluid" />
    </div>
    <div class="right-side">
      <a href="/" class="btn btn-secondary logout-button"
        ><i class="bi bi-box-arrow-left"></i>Voltar</a
      >
      <!-- Bloco que agrega os botões e o título -->
      <div class="button-container">
        <div class="title">Veja suas metas!</div>
        <div class="button-group">
          <a href="/aberto" class="btn btn-primary btn-em-aberto">
            <i class="bi bi-list-task"></i> Em Aberto
          </a>
          <a href="/atrasado" class="btn btn-primary btn-em-atraso">
            <i class="bi bi-exclamation-triangle"></i> Em Atraso
          </a>
          <a href="/concluido" class="btn btn-primary btn-concluido">
            <i class="bi bi-check2-circle"></i> Concluído
          </a>
        </div>
      </div>
    </div>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7"
    ></script>
  </body>
</html>
