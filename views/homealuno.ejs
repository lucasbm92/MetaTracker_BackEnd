<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
    <title>META TRACKER</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background: linear-gradient(to right, #ff7e5f, #feb47b);
        color: #333;
        margin: 0;
        padding: 0;
      }
      .content {
        display: flex;
        height: 100vh;
        overflow: hidden;
      }
      .left-half {
        background-image: url("img/az.jpg");
        background-size: cover;
        background-position: center;
        box-shadow: 0px 0 20px rgba(0, 0, 0, 0.458);
        width: 30%;
        height: 100%;
        display: flex;
        justify-content: center;
        align-items: center;
        border-end-end-radius: 30px;
        border-start-end-radius: 30px;
        animation: slideRight 2s forwards;
      }
      @keyframes slideRight {
        from {
          transform: translateX(-100%);
        }
        to {
          transform: translateX(0%);
        }
      }
      .right-half {
        flex: 7;
        display: flex;
        justify-content: center;
        align-items: center;
      }
      .btn-container {
        position: relative;
        display: inline-block;
        margin: 0 10px;
      }
      .btn-tooltip {
        display: none;
        position: absolute;
        background-color: rgba(0, 0, 0, 0.7);
        color: white;
        border-radius: 5px;
        padding: 3px 6px;
        font-size: 12px;
        top: -30px;
        left: 50%;
        transform: translateX(-50%);
      }
      .btn-container:hover .btn-tooltip {
        display: block;
      }
      .btn {
        padding: 40px 60px;
        margin: 20px;
        border: none;
        border-radius: 10px;
        color: white;
        font-weight: bold;
        cursor: pointer;
        transition: transform 0.3s ease;
      }
      .btn:hover {
        transform: scale(1.1);
      }
      .img-fluid {
        max-width: 80%;
        height: auto;
      }
    </style>
  </head>
  <body>
    <%- include('navbar') %>
    <div class="content">
      <div class="left-half">
        <img src="img/icon_txt.png" alt="Icon" class="img-fluid" />
      </div>
      <div class="right-half">
        <div class="main-content">
          <div class="btn-container">
            <a href="/alunometasaberto" class="btn btn-primary btn-lg"
              >Ver Metas</a
            >
            <div class="btn-tooltip">visualize metas em aberto</div>
          </div>
          <div class="btn-container">
            <a href="/alunometasconcluido" class="btn btn-primary btn-lg"
              >Metas Concluídas</a
            >
            <div class="btn-tooltip">visualize suas metas concluídas</div>
          </div>
        </div>
      </div>
    </div>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
